plugins {
    id 'org.asciidoctor.convert' version '2.2.0'
}

wrapper {
    gradleVersion = '5.0'
}

group = 'fr.jmini.example'
version = '1.0.0-SNAPSHOT'

asciidoctorj {
    version = '2.0.0'
}

// tag::task[]
asciidoctor {
    sourceDir = file('docs')
    sources {
        include 'index.adoc'
    }     
    outputDir = file('build/generated-docs')
    attributes = ['project-version'      : "$version",
                  'attribute-missing'    : 'warn',
                  'toc'                  : 'left',
                  'icons'                : 'font',
                  'sectanchors'          : 'true',
                  'idprefix'             : '',
                  'idseparator'          : '-']
    repositories {
        mavenCentral()
    }
    dependencies {
        asciidoctor 'fr.jmini.asciidoctorj:dynamic-include:1.1.0' // <1>
    }
}
// end::task[]